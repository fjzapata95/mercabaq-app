import { theme } from "@core/theme";

export const Style = {
    shadowStyle: {
        shadowColor: theme.colors.onSurface,
        shadowOpacity: 0.1,
        shadowRadius: 5,
        elevation: 5
    }
}